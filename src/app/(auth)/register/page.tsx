import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export default function RegisterPage() {
  return (
    <PlaceholderPage
      title="Register"
      description="Registration form placeholder. The authentication lifecycle is backed by Better Auth and PostgreSQL, not Supabase Auth."
    />
  );
}
