import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export default function ForgotPasswordPage() {
  return (
    <PlaceholderPage
      title="Forgot Password"
      description="Forgot-password placeholder. Reset token generation must store hashes only and use the transactional email adapter."
    />
  );
}
