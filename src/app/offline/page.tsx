import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export default function OfflinePage() {
  return (
    <PlaceholderPage
      title="Offline"
      description="Public shell resources can load offline. Private dashboards and learning data require a network connection."
    />
  );
}
