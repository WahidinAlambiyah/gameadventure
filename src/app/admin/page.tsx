import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export default function AdminPage() {
  return (
    <PlaceholderPage
      title="Admin"
      description="RBAC-protected admin foundation for content, users, roles, audit logs, and security events."
    />
  );
}
