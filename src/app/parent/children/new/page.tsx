import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export default function NewChildPage() {
  return (
    <PlaceholderPage
      title="New Child Profile"
      description="Create a minimal child profile with nickname, age range or birth year, avatar selection, and learning preferences only."
    />
  );
}
