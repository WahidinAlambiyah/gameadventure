import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export default function VerifyParentPinPage() {
  return (
    <PlaceholderPage
      title="Verify Parent PIN"
      description="Parent-gate verification placeholder for exiting child mode. It cannot restore an expired login session."
    />
  );
}
