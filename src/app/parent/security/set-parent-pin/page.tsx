import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export default function SetParentPinPage() {
  return (
    <PlaceholderPage
      title="Set Parent PIN"
      description="Four-digit PIN setup placeholder. Store only Argon2id hashes and enforce failed-attempt lockout."
    />
  );
}
