import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export default function ResultPage() {
  return (
    <PlaceholderPage
      title="Result"
      description="Result placeholder. Coins, energy, correctness, and completion status must be calculated server-side."
    />
  );
}
